<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="wrap_content">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:singleLine="true"
        android:inputType="textEmailAddress"
        android:hint="Username"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:id="@+id/fragment_login_username"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:singleLine="true"
        android:inputType="textPassword"
        android:hint="Password"
        android:layout_marginBottom="8dp"
        android:layout_below="@id/fragment_login_username"
        android:id="@+id/fragment_login_password"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Login"
        android:id="@+id/fragment_login_login"
        android:layout_below="@id/fragment_login_password"
        android:background="@color/dark_primary_color"/>

    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/fragment_login_progressBar"
        android:layout_alignTop="@id/fragment_login_login"
        android:layout_alignBottom="@id/fragment_login_login"
        android:layout_centerHorizontal="true"
        android:indeterminateOnly="true"
        android:visibility="gone"/>

</RelativeLayout>